const express = require("express");
const { body } = require("express-validator");

const chartController = require("../controller/chart/chart");

const router = express.Router();



module.exports = router;
